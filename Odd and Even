Question:

Write a program to check whether a number is even or odd.

Solution:

#include <iostream>
using namespace std;

int main() {
    int x;
    cout<<"Enter your value to check whether the number is odd or even: ";
    cin>>x;
    if(x%2==0){
        cout<<"The number is even";
    }
    else{
        cout<<"The number is odd";
    };
      
    return 0;
}
