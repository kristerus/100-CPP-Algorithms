Question:

Create a program to find the factorial of a number using iteration (loops).

Solution:

#include <vector>
#include <algorithm>
#include <string>
#include <iostream>
using namespace std;


int main() {
    int n;
    cout<<"Enter the number to find its factorial: ";
    cin>>n;
    int result=1;
    for(int i=i=1;i<=n;i++){
        result*=i;
    }
    cout<<result;
    return 0;
}
